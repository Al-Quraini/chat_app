name: chat_app
description: A Chat Flutter project.

publish_to:

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^1.1.1
  equatable: ^2.0.3
  bubble: ^1.2.1
  file_picker: ^3.0.1
  timeago: ^3.0.2
  image_picker: ^0.7.4
  firebase_auth: ^1.0.3
  firebase_auth_web: ^1.0.5
  firebase_storage: ^8.0.3
  firebase_storage_web: ^1.0.3
  cloud_firestore: ^2.1.0
  cloud_firestore_platform_interface: ^5.0.1
  enum_to_string: ^2.0.1
  intl: ^0.17.0
  dependencies:










dev_dependencies:
  json_serializable: ^4.1.0

  flutter_test:

    sdk: flutter


flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/


